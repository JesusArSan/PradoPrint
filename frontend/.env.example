# URL del backend para la SPA.
# Vacío en desarrollo: Vite proxy enruta /api y /public a VITE_DEV_PROXY_TARGET.
# En producción: definir con el origen del backend si NO comparten dominio.
# Ejemplo: VITE_API_URL=https://api.pradoprint.example.com
VITE_API_URL=

# Solo desarrollo: backend al que apunta el proxy de Vite.
VITE_DEV_PROXY_TARGET=http://localhost:3000
